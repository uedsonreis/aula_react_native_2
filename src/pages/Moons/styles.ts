import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    input: {
        height: 50,
        fontSize: 18,
        paddingLeft: 10,
        marginBottom: 10,
        borderBottomWidth: 1,
    },
})