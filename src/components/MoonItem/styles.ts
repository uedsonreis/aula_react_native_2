import { StyleSheet } from "react-native"

export default StyleSheet.create({

    container: {
        marginTop: 10,
        padding: 5,
    },

    text: {
        fontSize: 16,
    },

    deleteContainer: {
        padding: 10,
        height: '100%',
        alignItems: 'center',
        backgroundColor: 'red',
        justifyContent: 'center',
    },

    deleteText: {
        color: 'white',
        fontWeight: "700",
    },

})